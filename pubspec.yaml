name: imam_ali
description: تطبيق حكم الإمام علي عليه السلام
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # المكتبات الأساسية
  cupertino_icons: ^1.0.2
  
  # مكتبات المميزات
  share_plus: ^7.2.2
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true